{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                {{ form_start(article) }}
                {{ form_rest(article) }}
                <button type="submit">Envoyer</button>
                {{ form_end(article) }}
            </div>
        </div>
    </div>
{% endblock %}